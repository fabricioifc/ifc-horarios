pt-BR:
  errors:
    messages:
      invalid_date: "não é uma data válida"
      invalid_time: "não é um horário válido"
      invalid_datetime: "não é uma data/hora válida"
      is_at: "deve ser exatamente %{restriction}"
      before: "deve ser anterior a %{restriction}"
      on_or_before: "deve ser igual ou anterior a %{restriction}"
      after: "deve ser superior a %{restriction}"
      on_or_after: "deve ser igual ou superior a %{restriction}"
  validates_timeliness:
    error_value_formats:
      date: '%d/%m/%Y'
      time: '%H:%M'
      datetime: '%d/%m/%Y %H:%M'
